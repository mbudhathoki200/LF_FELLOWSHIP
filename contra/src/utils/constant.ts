type canvas = {
  WIDTH: number;
  HEIGHT: number;
};

export const CANVAS: canvas = {
  WIDTH: 1000,
  HEIGHT: 500,
};

type map = {
  WIDTH: number;
};

export const MAP: map = {
  WIDTH: 6500,
};

type player = {
  WIDTH: number;
  HEIGHT: number;
  SPEED: number;
};

export const PLAYER: player = {
  WIDTH: 50,
  HEIGHT: 75,
  SPEED: 15,
};
